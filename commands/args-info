module.exports = {
	name: '',
    description: '',
    guildOnly: false,
    args: false,
    aliases: [''],
	usage: '',
	cooldown: 0,
	execute(message, args) {
        const { commands } = message.client;
        message.channel.send(`Command name: ${ commands }\nArguments: ${args}`);
	},
};